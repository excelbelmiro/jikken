# 第 4 回授業課題

## 適応的学習支援 AI システム開発

**所要時間**: 180 分（説明 15 分 + 開発 165 分）

**成果物の提出先**: GitHub リポジトリ

---

## 🎯 今日の目標

第 1 回〜第 3 回で学んだ技術を組み合わせて、**自分だけの適応的学習支援 AI**を作る

**完成するもの**: 学習者の状態に合わせて支援を変える AI システム

---

## 📅 タイムスケジュール

| 時間           | 内容                                 |
| -------------- | ------------------------------------ |
| **0-15 分**    | 説明（適応的学習支援とは・課題説明） |
| **15-40 分**   | 設計（課題選択・知識ベース設計）     |
| **40-150 分**  | 開発（実装）                         |
| **150-180 分** | テスト・仕上げ・提出                 |

---

## STEP 0: 準備（Web 検索 API の設定）

### 0-1. なぜ API キーの管理が重要か

ファクトチェック機能で Web 検索を使います。そのために API キーが必要ですが、**API キーは「パスワード」のようなもの**です。

```
⚠️ APIキーをGitHubに公開すると...
・他人に勝手に使われる
・無料枠を使い切られる
・悪用される可能性がある
```

**解決策**: API キーを別ファイルに分けて、GitHub にプッシュしない

```
config.js    → GitHubにプッシュする（APIキーなし）
secrets.js   → GitHubにプッシュしない（APIキーここ）
.gitignore   → secrets.jsを除外する設定
```

---

### 0-2. Tavily API キーの取得（10 分）

**Tavily**: AI/RAG 用に最適化された Web 検索 API（月 1,000 回無料）

**手順**:

1. https://tavily.com/ にアクセス

2. 「Get API Key」または「Sign Up」をクリック

3. メールアドレスとパスワードを入力して登録

4. メール認証（届いたメールのリンクをクリック）

5. ダッシュボードで **API キー** をコピー
   - `tvly-` で始まる文字列
   - 例: `tvly-AbCdEfGhIjKlMnOp123456`

---

### 0-3. secrets.js を作成する

`js/secrets.js` を**新規作成**:

```javascript
// js/secrets.js - APIキー（絶対にGitHubにプッシュしない！）

const SECRETS = {
  tavilyApiKey: "tvly-ここにあなたのAPIキーを貼り付け",
};
```

**⚠️ 重要**: このファイルは自分の PC だけに保存します

---

### 0-4. .gitignore を設定する

プロジェクトのルートにある `.gitignore` を開いて、以下を**追加**:

```
# APIキーを含むファイル（絶対にプッシュしない）
js/secrets.js
secrets.js
```

**確認方法**:

```bash
git status
```

→ `secrets.js` が表示されなければ OK

---

### 0-5. HTML で secrets.js を読み込む

`index.html` の `<script>` タグに追加:

```html
<!-- 依存スクリプト -->
<script src="js/config.js"></script>
<script src="js/secrets.js"></script>
<!-- ← 追加 -->
<script src="js/llm-client.js"></script>
<!-- ... 以下略 -->
```

---

### 0-6. 動作確認

ブラウザでページを開き、F12（開発者ツール）→ Console で:

```javascript
console.log(SECRETS.tavilyApiKey);
```

→ 自分の API キーが表示されれば OK！

---

## STEP 1: 適応的学習支援とは

### 1-1. 固定的 vs 適応的

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   固定的な学習支援              適応的な学習支援            │
│   ──────────────              ──────────────               │
│   全員に同じ説明        →      理解度に応じた説明          │
│   一律の難易度          →      前提知識を確認して調整      │
│   単発の回答            →      関連概念へ誘導              │
│   情報をそのまま提示    →      信頼性を検証できる          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 1-2. 学習支援の 3 つの目的

| 目的           | 説明             | 例                           |
| -------------- | ---------------- | ---------------------------- |
| **知識獲得**   | 正確な情報を得る | 「○○ とは何か」を理解する    |
| **スキル習得** | できるようになる | 自分で問題を解けるようになる |
| **態度形成**   | 学び続けたくなる | 「次も学びたい」と思える     |

```
【態度形成の要素（ARCSモデル）】
A: Attention（注意）    → 興味を引く、好奇心を刺激
R: Relevance（関連性）  → 「自分に関係ある」と感じる
C: Confidence（自信）   → 「できそう」と思える
S: Satisfaction（満足） → 達成感、理解の実感
```

### 1-3. 技術と適応的支援の対応

**RAG の役割：「何を教えるか」を適応させる**

| 学習者の状態   | RAG の適応             |
| -------------- | ---------------------- |
| 初めて質問する | 基礎的な資料を優先検索 |
| 詳しく知りたい | 専門的な資料を検索     |
| 具体例がほしい | 事例・サンプルを検索   |

**オントロジーの役割：「どう教えるか」を適応させる**

| 学習者の状態     | オントロジーの適応                |
| ---------------- | --------------------------------- |
| 前提知識が不足   | 「先に X を理解しましょう」と誘導 |
| 基礎は理解済み   | 発展的な関連概念を提示            |
| 全体像が見えない | 概念間の繋がりを示す              |

**ファクトチェックの役割：知識の信頼性を担保する**

| 状況                 | ファクトチェックの役割    |
| -------------------- | ------------------------- |
| 知識ベースが古い     | 最新情報を Web 検索で取得 |
| 情報が怪しい         | 論文・書籍で検証          |
| 学習者が疑問を持った | 自分で検証する手段を提供  |

---

## STEP 2: 課題を選択する

### 3 つの課題から 1 つ選んでください

---

### 【課題 1】🎄 冬休み特訓プランナー

**コンセプト**: 「○○ について学びたい」→ 自分に合った学習計画を立てる

```
例: 「冬休み2週間でLLMについて学びたい」

  ┌─────────────────────────────────────────────┐
  │ 📝 まず現状を確認させてください            │
  │                                             │
  │ Q1. Pythonは書けますか？                   │
  │ Q2. 機械学習の経験はありますか？           │
  │ Q3. 1日何時間くらい勉強できますか？        │
  └─────────────────────────────────────────────┘
                    ↓
  ┌─────────────────────────────────────────────┐
  │ 📚 あなたへの学習プラン                     │
  │                                             │
  │ 【前提知識チェック】                        │
  │ LLMを学ぶには、ニューラルネットの基礎が     │
  │ 必要です。まずそこから始めましょう。       │
  │                                             │
  │ 【2週間プラン】                             │
  │ Week1: Python復習 → NN基礎                 │
  │ Week2: Transformer → LLM実践               │
  │                                             │
  │ 【おすすめリソース】                        │
  │ ・動画: ○○チャンネル（初心者向け）         │
  │ ・書籍: ○○入門                            │
  └─────────────────────────────────────────────┘
```

**適応的支援のポイント**:

| 支援         | 実現方法                                            |
| ------------ | --------------------------------------------------- |
| 前提知識確認 | オントロジーで「LLM → NN → Python」の依存関係を定義 |
| レベル適応   | 初心者/経験者で推薦リソースを変える                 |
| 関連提示     | 「LLM を学んだら、RAG も面白いよ」                  |
| 検証機能     | 推薦したリソースが最新か確認できる                  |

---

### 【課題 2】🔧 ものづくりスターター AI

**コンセプト**: 「何か作りたいけど何から始めれば...」→ 最初の一歩を踏み出せる

```
例: 「電子工作やってみたいけど難しそう」

  ┌─────────────────────────────────────────────┐
  │ 🤖 なるほど！まず教えてください            │
  │                                             │
  │ Q1. はんだ付けしたことありますか？         │
  │ Q2. プログラミングは得意？苦手？           │
  │ Q3. 予算はどれくらい？                     │
  └─────────────────────────────────────────────┘
                    ↓
  ┌─────────────────────────────────────────────┐
  │ 💡 あなたにおすすめの最初の一歩             │
  │                                             │
  │ はんだ付け未経験なら、まずは                │
  │ 「ブレッドボード」から始めましょう！       │
  │ はんだ不要で電子回路を試せます。           │
  │                                             │
  │ 【ステップ1】LED点灯（30分で完成）         │
  │  必要なもの: LED、抵抗、電池、ボード       │
  │  予算: 約500円                              │
  │                                             │
  │ 【ステップ2】ボタンでLED制御               │
  │ 【ステップ3】Arduino導入                   │
  │                                             │
  │ 🎯 これができたら「センサーで遊ぶ」に進もう │
  └─────────────────────────────────────────────┘
```

**適応的支援のポイント**:

| 支援         | 実現方法                                 |
| ------------ | ---------------------------------------- |
| 前提知識確認 | 経験に応じてスタート地点を調整           |
| 段階的提示   | いきなり完成形を見せず、小さなステップで |
| 関連提示     | 「電子工作 → Arduino → IoT」の発展を示す |
| 検証機能     | 材料の価格・入手先が正しいか確認         |

---

### 【課題 3】🎌 祝日マスター AI

**コンセプト**: 使うほど祝日に詳しくなり、過ごし方が豊かになる

```
例: 「建国記念の日って何する日？」

  ┌─────────────────────────────────────────────┐
  │ 📖 建国記念の日（2月11日）                  │
  │                                             │
  │ 【由来】                                     │
  │ 日本書紀で神武天皇が即位したとされる日。   │
  │ 1966年に「建国をしのび、国を愛する心を     │
  │ 養う日」として制定されました。             │
  │                                             │
  │ 【関連する祝日】                            │
  │ ・天皇誕生日（皇室関連）                   │
  │ ・文化の日（明治天皇誕生日が由来）         │
  │                                             │
  │ 【過ごし方アイデア】                        │
  │ 🏛️ 歴史スポット巡り                         │
  │ 🎬 日本の歴史映画を観る                     │
  │ 🍳 伝統料理を作ってみる                     │
  └─────────────────────────────────────────────┘
                    ↓
  ┌─────────────────────────────────────────────┐
  │ 💡 もっと知りたい？                         │
  │                                             │
  │ → 他の国の建国記念日と比べてみる？         │
  │ → 次の祝日「春分の日」について知る？       │
  │ → 祝日と季節行事の関係を見る？             │
  └─────────────────────────────────────────────┘
```

**適応的支援のポイント**:

| 支援       | 実現方法                                |
| ---------- | --------------------------------------- |
| 難易度適応 | 子供向け説明 / 詳しい歴史解説を切り替え |
| 関連提示   | 祝日同士の繋がり、季節行事との関係      |
| 興味拡張   | 「この祝日に興味があるなら ○○ も」      |
| 検証機能   | 由来の正確性を確認、最新の祝日情報      |

---

## STEP 3: 必須要件を確認する

### すべての課題で実装必須

| 要件         | 技術             | 目的                                      |
| ------------ | ---------------- | ----------------------------------------- |
| ① 知識検索   | RAG              | 学習資料から適切な情報を取得              |
| ② 適応的支援 | オントロジー     | 学習者に合わせた支援（下記から 1 つ以上） |
| ③ 情報検証   | ファクトチェック | 信頼性の担保                              |

**② 適応的支援の選択肢（1 つ以上実装）**:

- **前提知識チェック**: 「先に X を学びましょう」と誘導
- **難易度適応**: 初心者/経験者で説明を変える
- **関連概念提示**: 「次は Y も関係しています」と拡張
- **段階的ヒント**: すぐ答えを言わず、少しずつ導く

---

## STEP 4: 設計する（25 分）

### 3-1. 設計シートを埋める

```
┌─────────────────────────────────────────────────────────────┐
│                      設計シート                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  選んだ課題: □ 1（特訓プランナー）                          │
│             □ 2（ものづくり）                               │
│             ☑️ 3（祝日マスター）                             │
│                                                             │
│  システム名: 外国人が日本を楽しむための祝日AI          │
│                                                             │
│  誰のためのシステム？: 日本に住み始めて1年以内の外国人        │
│  （例: プログラミング初心者、電子工作に興味ある人）          │
│                                                             │
│  知識ベースに入れる情報:                                    │
│  ・日本独自の祝日                         │
│  ・外国の祝日との関係              │
│  ・おすすめの過ごし方                         │
│                                                             │
│  オントロジーで定義する概念関係:                            │
│  ・「」の前提知識は「____」                             │
│  ・「____」と「____」は関連している                         │
│  ・「____」の次は「____」を学ぶ                             │
│                                                             │
│  実装する適応的支援（1つ以上チェック）:                     │
│  □ 前提知識チェック                                        │
│  □ 難易度適応                                              │
│  □ 関連概念提示                                            │
│  □ 段階的ヒント                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3-2. ファイル構成を決める

```
session4-project/
├── index.html                 ← メインUI
├── js/
│   ├── config.js             ← API設定（既存）
│   ├── llm-client.js         ← LLMクライアント（既存）
│   ├── vector-search.js      ← ベクトル検索（既存）
│   ├── ontology.js           ← オントロジー（既存）
│   ├── semantic-rag.js       ← セマンティックRAG（既存）
│   ├── web-search-client.js  ← Web検索（既存）
│   ├── fact-checker.js       ← ファクトチェック（新規）
│   └── my-app.js             ← 自分のアプリ（新規）
├── data/
│   ├── my-documents.json     ← 知識ベース（新規）
│   └── my-ontology.json      ← オントロジー（新規）
└── css/
    └── style.css             ← スタイル
```

---

## STEP 5: 知識ベースを作成する

### 4-1. 知識ベースのテンプレート

`data/my-documents.json` を作成:

```json
{
  "documents": [
    {
      "id": "doc001",
      "title": "タイトル",
      "content": "本文をここに書く。詳しく書くほど検索精度が上がる。",
      "category": "カテゴリ",
      "level": "beginner",
      "prerequisites": ["前提知識1", "前提知識2"],
      "keywords": ["キーワード1", "キーワード2"]
    }
  ]
}
```

### 4-2. 課題別の知識ベース例

**課題 1: 冬休み特訓プランナー**

```json
{
  "documents": [
    {
      "id": "learn001",
      "title": "Python入門の学習ステップ",
      "content": "Python入門は、まず変数と型の理解から始める。次に制御構文（if, for）、そして関数の定義へ進む。目安として、1日2時間で1週間程度。",
      "category": "programming",
      "level": "beginner",
      "prerequisites": [],
      "estimatedTime": "1週間"
    },
    {
      "id": "learn002",
      "title": "ニューラルネットワーク基礎",
      "content": "ニューラルネットワークを学ぶには、まずPythonの基礎とNumPyの使い方が必要。パーセプトロンから始め、多層化、誤差逆伝播法へ進む。",
      "category": "ai",
      "level": "intermediate",
      "prerequisites": ["Python基礎", "NumPy"],
      "estimatedTime": "2週間"
    }
  ]
}
```

**課題 2: ものづくりスターター AI**

```json
{
  "documents": [
    {
      "id": "make001",
      "title": "LED点灯プロジェクト",
      "content": "最も簡単な電子工作。LED、抵抗（330Ω）、電池、ブレッドボードがあれば30分で完成。はんだ付け不要。成功体験を得るのに最適。",
      "category": "electronics",
      "level": "beginner",
      "prerequisites": [],
      "materials": ["LED", "抵抗", "電池", "ブレッドボード"],
      "estimatedCost": 500,
      "estimatedTime": "30分"
    },
    {
      "id": "make002",
      "title": "Arduino入門",
      "content": "マイコンを使った電子工作の入門。LEDの点滅（Lチカ）から始める。プログラミングで制御できる楽しさを体験。",
      "category": "electronics",
      "level": "intermediate",
      "prerequisites": ["LED点灯", "基本的なプログラミング"],
      "materials": ["Arduino Uno", "USBケーブル", "LED"],
      "estimatedCost": 3000
    }
  ]
}
```

**課題 3: 祝日マスター AI**

```json
{
  "documents": [
    {
      "id": "holiday001",
      "title": "建国記念の日",
      "content": "2月11日。日本書紀で神武天皇が即位したとされる日（紀元前660年）。1966年に制定。建国をしのび、国を愛する心を養う日とされる。",
      "category": "national_holiday",
      "date": "2月11日",
      "origin": "神武天皇即位",
      "established": 1966,
      "relatedHolidays": ["天皇誕生日", "文化の日"],
      "activities": ["歴史スポット巡り", "日本史の映画鑑賞"]
    },
    {
      "id": "holiday002",
      "title": "春分の日",
      "content": "3月20日頃（年により変動）。自然をたたえ、生物をいつくしむ日。昼と夜の長さがほぼ等しくなる。お彼岸の中日でもある。",
      "category": "national_holiday",
      "date": "3月20日頃",
      "origin": "二十四節気",
      "relatedHolidays": ["秋分の日"],
      "activities": ["お墓参り", "ぼたもち作り", "自然観察"]
    }
  ]
}
```

---

## STEP 6: オントロジーを作成する

### 5-1. オントロジーのテンプレート

`data/my-ontology.json` を作成:

```json
{
  "concepts": {
    "concept_id": {
      "label": "表示名",
      "description": "説明",
      "level": "beginner/intermediate/advanced",
      "prerequisites": ["前提となる概念ID"],
      "relatedConcepts": ["関連する概念ID"],
      "nextSteps": ["次に学ぶべき概念ID"]
    }
  },
  "relations": [
    {
      "from": "概念ID",
      "to": "概念ID",
      "type": "prerequisite/related/nextStep",
      "strength": 0.8
    }
  ]
}
```

### 5-2. 課題別のオントロジー例

**課題 1: 冬休み特訓プランナー（LLM 学習）**

```json
{
  "concepts": {
    "python": {
      "label": "Python",
      "description": "プログラミング言語の基礎",
      "level": "beginner",
      "prerequisites": [],
      "relatedConcepts": ["numpy"],
      "nextSteps": ["numpy", "neural-network"]
    },
    "numpy": {
      "label": "NumPy",
      "description": "数値計算ライブラリ",
      "level": "beginner",
      "prerequisites": ["python"],
      "relatedConcepts": ["neural-network"],
      "nextSteps": ["neural-network"]
    },
    "neural-network": {
      "label": "ニューラルネットワーク",
      "description": "機械学習の基礎モデル",
      "level": "intermediate",
      "prerequisites": ["python", "numpy"],
      "relatedConcepts": ["deep-learning"],
      "nextSteps": ["transformer"]
    },
    "transformer": {
      "label": "Transformer",
      "description": "Attention機構を使ったアーキテクチャ",
      "level": "intermediate",
      "prerequisites": ["neural-network"],
      "relatedConcepts": ["llm"],
      "nextSteps": ["llm"]
    },
    "llm": {
      "label": "LLM",
      "description": "大規模言語モデル",
      "level": "advanced",
      "prerequisites": ["transformer"],
      "relatedConcepts": ["rag", "prompt-engineering"],
      "nextSteps": ["rag", "fine-tuning"]
    },
    "rag": {
      "label": "RAG",
      "description": "検索拡張生成",
      "level": "advanced",
      "prerequisites": ["llm"],
      "relatedConcepts": ["vector-search"],
      "nextSteps": []
    }
  },
  "relations": [
    {
      "from": "python",
      "to": "numpy",
      "type": "prerequisite",
      "strength": 0.9
    },
    {
      "from": "numpy",
      "to": "neural-network",
      "type": "prerequisite",
      "strength": 0.8
    },
    {
      "from": "neural-network",
      "to": "transformer",
      "type": "prerequisite",
      "strength": 0.9
    },
    {
      "from": "transformer",
      "to": "llm",
      "type": "prerequisite",
      "strength": 0.95
    },
    { "from": "llm", "to": "rag", "type": "nextStep", "strength": 0.8 }
  ]
}
```

**課題 2: ものづくりスターター AI**

```json
{
  "concepts": {
    "led-basic": {
      "label": "LED点灯",
      "description": "最も基本的な電子工作",
      "level": "beginner",
      "prerequisites": [],
      "nextSteps": ["led-control", "breadboard"]
    },
    "breadboard": {
      "label": "ブレッドボード",
      "description": "はんだ不要で回路を試せるボード",
      "level": "beginner",
      "prerequisites": [],
      "relatedConcepts": ["led-basic"]
    },
    "led-control": {
      "label": "LED制御",
      "description": "ボタンやセンサーでLEDを制御",
      "level": "beginner",
      "prerequisites": ["led-basic"],
      "nextSteps": ["arduino"]
    },
    "arduino": {
      "label": "Arduino",
      "description": "プログラムで制御するマイコン",
      "level": "intermediate",
      "prerequisites": ["led-control", "programming-basic"],
      "nextSteps": ["sensor", "iot"]
    },
    "sensor": {
      "label": "センサー活用",
      "description": "温度、光、距離などを検出",
      "level": "intermediate",
      "prerequisites": ["arduino"],
      "nextSteps": ["iot"]
    },
    "iot": {
      "label": "IoT",
      "description": "インターネットに接続するものづくり",
      "level": "advanced",
      "prerequisites": ["arduino", "sensor", "network-basic"],
      "nextSteps": []
    }
  }
}
```

**課題 3: 祝日マスター AI**

```json
{
  "concepts": {
    "kenkoku": {
      "label": "建国記念の日",
      "description": "2月11日・建国をしのぶ日",
      "category": "national",
      "season": "winter",
      "relatedConcepts": ["tennou-tanjoubi", "bunka-no-hi"],
      "themes": ["歴史", "日本の成り立ち"]
    },
    "tennou-tanjoubi": {
      "label": "天皇誕生日",
      "description": "2月23日・天皇の誕生日を祝う",
      "category": "national",
      "season": "winter",
      "relatedConcepts": ["kenkoku", "bunka-no-hi"]
    },
    "shunbun": {
      "label": "春分の日",
      "description": "3月20日頃・自然をたたえる",
      "category": "nature",
      "season": "spring",
      "relatedConcepts": ["shubun", "ohigan"],
      "themes": ["自然", "お彼岸"]
    },
    "shubun": {
      "label": "秋分の日",
      "description": "9月23日頃・祖先をうやまう",
      "category": "nature",
      "season": "autumn",
      "relatedConcepts": ["shunbun", "ohigan"]
    },
    "ohigan": {
      "label": "お彼岸",
      "description": "春分・秋分を中心とした7日間",
      "category": "tradition",
      "relatedConcepts": ["shunbun", "shubun"]
    }
  }
}
```

---

## STEP 7: Web 検索クライアントを作成する

### 7-1. web-search-client.js を作成

`js/web-search-client.js`:

```javascript
// js/web-search-client.js - Tavily APIを使ったWeb検索

class WebSearchClient {
  constructor() {
    // secrets.js から APIキーを取得
    if (typeof SECRETS === "undefined" || !SECRETS.tavilyApiKey) {
      console.error("❌ SECRETS.tavilyApiKey が設定されていません");
      console.error(
        "→ js/secrets.js を作成して、Tavily APIキーを設定してください"
      );
    }
    this.apiKey = SECRETS?.tavilyApiKey || "";
  }

  async search(query, maxResults = 5) {
    if (!this.apiKey) {
      throw new Error(
        "Tavily APIキーが設定されていません。secrets.jsを確認してください。"
      );
    }

    try {
      const response = await fetch("https://api.tavily.com/search", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          api_key: this.apiKey,
          query: query,
          search_depth: "basic",
          max_results: maxResults,
          include_answer: false,
          include_raw_content: false,
        }),
      });

      if (!response.ok) {
        const errorText = await response.text();
        console.error("Tavily API Error:", errorText);
        throw new Error("Web検索に失敗しました");
      }

      const data = await response.json();

      return {
        query: query,
        results:
          data.results?.map((r) => ({
            title: r.title || "",
            url: r.url || "",
            snippet: r.content || "",
          })) || [],
        retrievedAt: new Date().toISOString(),
      };
    } catch (error) {
      console.error("Web検索エラー:", error);
      throw error;
    }
  }
}
```

### 7-2. 動作確認

ブラウザのコンソール（F12）で:

```javascript
const ws = new WebSearchClient();
ws.search("プログラミング 入門").then(console.log);
```

成功すると:

```javascript
{
  query: "プログラミング 入門",
  results: [
    { title: "...", url: "...", snippet: "..." },
    ...
  ],
  retrievedAt: "2025-..."
}
```

---

## STEP 8: ファクトチェック機能を実装する

### 8-1. fact-checker.js を作成

`js/fact-checker.js`:

```javascript
// js/fact-checker.js - 情報検証機能

class FactChecker {
  constructor() {
    this.webSearch = new WebSearchClient();
    this.llm = new EducationLLMClient(API_CONFIG.studentId);
  }

  // 論文・学術情報で検証
  async verifyWithAcademic(query) {
    const academicQuery = `${query} 論文 研究 学術`;
    const results = await this.webSearch.search(academicQuery);
    return {
      type: "academic",
      query: academicQuery,
      results: results.results,
      retrievedAt: results.retrievedAt,
    };
  }

  // 書籍で検証
  async verifyWithBooks(query) {
    const bookQuery = `${query} 書籍 本 入門`;
    const results = await this.webSearch.search(bookQuery);
    return {
      type: "books",
      query: bookQuery,
      results: results.results,
      retrievedAt: results.retrievedAt,
    };
  }

  // 一般Webで検証
  async verifyWithWeb(query) {
    const results = await this.webSearch.search(query);
    return {
      type: "web",
      query: query,
      results: results.results,
      retrievedAt: results.retrievedAt,
    };
  }

  // 検証結果を踏まえて再評価
  async evaluateWithSources(originalQuestion, originalAnswer, searchResults) {
    const sourceSummary = searchResults.results
      .slice(0, 3)
      .map((r, i) => `[${i + 1}] ${r.title}: ${r.snippet}`)
      .join("\n");

    const prompt = `
あなたは情報の正確性を評価する専門家です。

【元の質問】
${originalQuestion}

【AIの回答】
${originalAnswer}

【検索で見つかった情報】
${sourceSummary}

【タスク】
1. 元の回答が正確かどうか評価してください
2. 検索結果と矛盾する点があれば指摘してください
3. より正確な情報があれば補足してください

【回答形式】
・正確性: ○正確 / △一部不正確 / ×不正確
・評価コメント: （理由を簡潔に）
・補足情報: （あれば）
`;

    const response = await this.llm.chat(prompt);
    return {
      evaluation: response.response,
      sources: searchResults.results.slice(0, 3),
      retrievedAt: searchResults.retrievedAt,
    };
  }

  // 検証ボタンのUIを生成
  generateVerificationUI(containerId) {
    const container = document.getElementById(containerId);
    if (!container) return;

    container.innerHTML = `
            <div class="verification-section">
                <p class="verification-prompt">💡 この情報を検証したいですか？</p>
                <div class="verification-buttons">
                    <button class="verify-btn" onclick="verifyInfo('academic')">
                        📄 論文で検証
                    </button>
                    <button class="verify-btn" onclick="verifyInfo('books')">
                        📚 書籍で検証
                    </button>
                    <button class="verify-btn" onclick="verifyInfo('web')">
                        🌐 Webで検証
                    </button>
                </div>
                <div id="verification-results"></div>
            </div>
        `;
  }

  // 検証結果を表示
  displayVerificationResults(results, evaluation) {
    const container = document.getElementById("verification-results");
    if (!container) return;

    container.innerHTML = `
            <div class="verification-result">
                <h4>🔍 検証結果</h4>
                <p class="retrieved-at">取得日時: ${new Date(
                  results.retrievedAt
                ).toLocaleString("ja-JP")}</p>
                
                <div class="sources-list">
                    ${results.results
                      .slice(0, 3)
                      .map(
                        (r, i) => `
                        <div class="source-item">
                            <strong>${i + 1}. ${r.title}</strong>
                            <p>${r.snippet}</p>
                            <a href="${r.url}" target="_blank">🔗 詳細を見る</a>
                        </div>
                    `
                      )
                      .join("")}
                </div>

                ${
                  evaluation
                    ? `
                    <div class="evaluation">
                        <h4>📋 評価</h4>
                        <div class="evaluation-content">
                            ${evaluation.evaluation.replace(/\n/g, "<br>")}
                        </div>
                    </div>
                `
                    : ""
                }
            </div>
        `;
  }
}
```

---

## STEP 9: メインアプリを作成する

### 9-1. アプリの基本構造

`js/my-app.js`:

```javascript
// js/my-app.js - メインアプリケーション

class MyLearningApp {
  constructor() {
    this.rag = null;
    this.ontology = null;
    this.factChecker = null;
    this.currentAnswer = null;
    this.currentQuestion = null;
  }

  async initialize() {
    console.log("🚀 システム初期化中...");

    // データ読み込み
    const [docsRes, ontoRes] = await Promise.all([
      fetch("data/my-documents.json"),
      fetch("data/my-ontology.json"),
    ]);

    const docs = await docsRes.json();
    const onto = await ontoRes.json();

    // 各コンポーネント初期化
    this.rag = new SemanticRAGSystem();
    await this.rag.initialize(docs.documents, onto);

    this.ontology = new LearningOntology();
    await this.ontology.loadOntology(onto);

    this.factChecker = new FactChecker();

    console.log("✅ 準備完了！");
  }

  // 質問処理
  async handleQuestion(question) {
    this.currentQuestion = question;

    // RAGで回答生成
    const result = await this.rag.semanticQuery(question);
    this.currentAnswer = result.answer;

    // 適応的支援を追加（ここをカスタマイズ）
    const adaptiveSupport = await this.generateAdaptiveSupport(
      question,
      result
    );

    return {
      answer: result.answer,
      sources: result.sources,
      adaptiveSupport: adaptiveSupport,
    };
  }

  // 適応的支援の生成（カスタマイズポイント）
  async generateAdaptiveSupport(question, ragResult) {
    const support = {};

    // 1. 前提知識チェック
    const concepts = ragResult.expandedQuery?.concepts || [];
    if (concepts.length > 0) {
      const prerequisites = [];
      for (const concept of concepts) {
        const prereqs = this.ontology.getPrerequisiteChain(concept);
        prerequisites.push(...prereqs);
      }
      if (prerequisites.length > 0) {
        support.prerequisites = {
          message:
            "💡 この内容を理解するには、以下の前提知識があると良いです：",
          items: [...new Set(prerequisites)],
        };
      }
    }

    // 2. 関連概念の提示
    if (concepts.length > 0) {
      const related = [];
      for (const concept of concepts) {
        const relatedConcepts = this.ontology.findRelatedConcepts(concept, 1);
        related.push(...relatedConcepts);
      }
      if (related.length > 0) {
        support.relatedConcepts = {
          message: "🔗 関連するトピック：",
          items: [...new Set(related)],
        };
      }
    }

    // 3. 次のステップ
    if (concepts.length > 0) {
      const nextSteps = [];
      for (const concept of concepts) {
        const conceptData = this.ontology.getConcept(concept);
        if (conceptData?.nextSteps) {
          nextSteps.push(...conceptData.nextSteps);
        }
      }
      if (nextSteps.length > 0) {
        support.nextSteps = {
          message: "📈 次に学ぶと良いこと：",
          items: [...new Set(nextSteps)],
        };
      }
    }

    return support;
  }

  // 結果表示
  displayResult(result, containerId) {
    const container = document.getElementById(containerId);
    if (!container) return;

    let html = `
            <div class="result-section">
                <div class="answer-box">
                    <h3>🤖 回答</h3>
                    <p>${result.answer.replace(/\n/g, "<br>")}</p>
                </div>
        `;

    // 適応的支援の表示
    if (result.adaptiveSupport) {
      const support = result.adaptiveSupport;

      if (support.prerequisites) {
        html += `
                    <div class="support-box prerequisites">
                        <p>${support.prerequisites.message}</p>
                        <ul>
                            ${support.prerequisites.items
                              .map((item) => `<li>${item}</li>`)
                              .join("")}
                        </ul>
                    </div>
                `;
      }

      if (support.relatedConcepts) {
        html += `
                    <div class="support-box related">
                        <p>${support.relatedConcepts.message}</p>
                        <div class="concept-tags">
                            ${support.relatedConcepts.items
                              .map((item) => `<span class="tag">${item}</span>`)
                              .join("")}
                        </div>
                    </div>
                `;
      }

      if (support.nextSteps) {
        html += `
                    <div class="support-box next-steps">
                        <p>${support.nextSteps.message}</p>
                        <ul>
                            ${support.nextSteps.items
                              .map((item) => `<li>${item}</li>`)
                              .join("")}
                        </ul>
                    </div>
                `;
      }
    }

    // 検証ボタン
    html += `
                <div id="verification-area"></div>
            </div>
        `;

    container.innerHTML = html;

    // 検証UIを追加
    this.factChecker.generateVerificationUI("verification-area");
  }
}

// グローバル変数
let app;
let currentQuestion;
let currentAnswer;

// 初期化
document.addEventListener("DOMContentLoaded", async () => {
  app = new MyLearningApp();
  await app.initialize();
  document.getElementById("ask-btn").disabled = false;
  document.getElementById("question-input").disabled = false;
});

// 質問送信
async function askQuestion() {
  const input = document.getElementById("question-input");
  const question = input.value.trim();
  if (!question) return;

  currentQuestion = question;
  document.getElementById("ask-btn").disabled = true;

  try {
    const result = await app.handleQuestion(question);
    currentAnswer = result.answer;
    app.displayResult(result, "result-container");
  } catch (error) {
    console.error("エラー:", error);
    alert("エラーが発生しました: " + error.message);
  } finally {
    document.getElementById("ask-btn").disabled = false;
  }
}

// 検証実行
async function verifyInfo(type) {
  if (!currentQuestion || !currentAnswer) return;

  const btn = event.target;
  btn.disabled = true;
  btn.textContent = "検証中...";

  try {
    let results;
    switch (type) {
      case "academic":
        results = await app.factChecker.verifyWithAcademic(currentQuestion);
        break;
      case "books":
        results = await app.factChecker.verifyWithBooks(currentQuestion);
        break;
      case "web":
        results = await app.factChecker.verifyWithWeb(currentQuestion);
        break;
    }

    const evaluation = await app.factChecker.evaluateWithSources(
      currentQuestion,
      currentAnswer,
      results
    );

    app.factChecker.displayVerificationResults(results, evaluation);
  } catch (error) {
    console.error("検証エラー:", error);
    alert("検証に失敗しました: " + error.message);
  } finally {
    btn.disabled = false;
    btn.textContent = btn.textContent.replace("検証中...", "");
  }
}
```

---

## STEP 10: HTML を作成する

`index.html`:

```html
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>適応的学習支援AI</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", "Hiragino Sans", sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        padding: 20px;
      }

      .container {
        max-width: 900px;
        margin: 0 auto;
        background: white;
        border-radius: 15px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
        padding: 30px;
      }

      h1 {
        color: #667eea;
        margin-bottom: 10px;
      }
      .subtitle {
        color: #666;
        margin-bottom: 25px;
      }

      .input-section {
        display: flex;
        gap: 10px;
        margin-bottom: 25px;
      }

      #question-input {
        flex: 1;
        padding: 15px;
        border: 2px solid #ddd;
        border-radius: 8px;
        font-size: 16px;
      }

      #question-input:focus {
        outline: none;
        border-color: #667eea;
      }

      .btn {
        padding: 15px 25px;
        border: none;
        border-radius: 8px;
        font-size: 16px;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.2s;
      }

      .btn:hover {
        transform: translateY(-2px);
      }
      .btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
      }

      .btn-primary {
        background: linear-gradient(135deg, #667eea, #764ba2);
        color: white;
      }

      .answer-box {
        background: #f8f9fa;
        padding: 20px;
        border-radius: 10px;
        border-left: 5px solid #667eea;
        margin-bottom: 20px;
      }

      .answer-box h3 {
        color: #667eea;
        margin-bottom: 10px;
      }
      .answer-box p {
        line-height: 1.8;
      }

      .support-box {
        padding: 15px;
        border-radius: 8px;
        margin-bottom: 15px;
      }

      .support-box.prerequisites {
        background: #fff3e0;
        border-left: 4px solid #ff9800;
      }

      .support-box.related {
        background: #e3f2fd;
        border-left: 4px solid #2196f3;
      }

      .support-box.next-steps {
        background: #e8f5e9;
        border-left: 4px solid #4caf50;
      }

      .concept-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        margin-top: 10px;
      }

      .tag {
        background: #2196f3;
        color: white;
        padding: 5px 12px;
        border-radius: 15px;
        font-size: 14px;
      }

      .verification-section {
        margin-top: 25px;
        padding: 20px;
        background: #fafafa;
        border-radius: 10px;
      }

      .verification-prompt {
        margin-bottom: 15px;
        color: #666;
      }

      .verification-buttons {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
      }

      .verify-btn {
        padding: 10px 20px;
        border: 2px solid #667eea;
        border-radius: 8px;
        background: white;
        color: #667eea;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.2s;
      }

      .verify-btn:hover {
        background: #667eea;
        color: white;
      }

      .verification-result {
        margin-top: 20px;
        padding: 15px;
        background: white;
        border-radius: 8px;
        border: 1px solid #ddd;
      }

      .source-item {
        padding: 10px;
        margin: 10px 0;
        background: #f5f5f5;
        border-radius: 5px;
      }

      .source-item a {
        color: #667eea;
      }

      .evaluation {
        margin-top: 15px;
        padding: 15px;
        background: #e8f5e9;
        border-radius: 8px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>🎓 適応的学習支援AI</h1>
      <p class="subtitle">あなたの状況に合わせてサポートします</p>

      <div class="input-section">
        <input
          type="text"
          id="question-input"
          placeholder="質問を入力してください"
          disabled
        />
        <button
          class="btn btn-primary"
          id="ask-btn"
          onclick="askQuestion()"
          disabled
        >
          質問する
        </button>
      </div>

      <div id="result-container"></div>
    </div>

    <!-- 依存スクリプト -->
    <script src="js/config.js"></script>
    <script src="js/secrets.js"></script>
    <!-- APIキー（GitHubにはプッシュしない） -->
    <script src="js/llm-client.js"></script>
    <script src="js/vector-search.js"></script>
    <script src="js/ontology.js"></script>
    <script src="js/concept-extractor.js"></script>
    <script src="js/semantic-rag.js"></script>
    <script src="js/web-search-client.js"></script>
    <script src="js/fact-checker.js"></script>
    <script src="js/my-app.js"></script>
  </body>
</html>
```

---

## ✅ 最終チェックリスト

### 必須項目

- [ ] **API キー設定**
  - [ ] Tavily API キーを取得した
  - [ ] `js/secrets.js` を作成した
  - [ ] `.gitignore` に `secrets.js` を追加した
- [ ] 知識ベース（`data/my-documents.json`）を作成した
- [ ] オントロジー（`data/my-ontology.json`）を作成した
- [ ] RAG で質問応答ができる
- [ ] 適応的支援機能が 1 つ以上動く
  - [ ] 前提知識チェック
  - [ ] 難易度適応
  - [ ] 関連概念提示
  - [ ] 段階的ヒント
- [ ] ファクトチェック機能が動く
- [ ] GitHub にプッシュした（**secrets.js が含まれていないことを確認！**）

### 発展項目

- [ ] 複数の適応的支援を実装
- [ ] UI がわかりやすい
- [ ] 独自の工夫がある
- [ ] 知識ベースが充実している（10 件以上）

---

## 💡 困ったときは

| 問題                           | 解決策                                       |
| ------------------------------ | -------------------------------------------- |
| 何から始めればいい？           | まず知識ベース JSON 5 件を作る               |
| 技術を忘れた                   | 第 3 回のコードをコピーして使う              |
| オントロジーの設計がわからない | 「A するには B が必要」という関係を 3 つ書く |
| エラーが出る                   | F12 でコンソール確認 → 先生に質問            |
| 時間が足りない                 | 必須項目のみに集中                           |
| Web 検索が動かない             | secrets.js の API キーを確認                 |
| API キーが無効と言われる       | Tavily のダッシュボードで再確認              |

---

## 📤 提出

### 提出前の確認（重要！）

```bash
# secrets.js がプッシュされないことを確認
git status
```

→ `secrets.js` が表示されていたら `.gitignore` を確認！

### コミット・プッシュ

1. 全ファイルを保存
2. GitHub にコミット・プッシュ

```bash
git add .
git commit -m "第4回: [システム名]を実装"
git push
```

### ⚠️ 確認

GitHub のリポジトリを開いて、`js/secrets.js` が**存在しないこと**を確認してください。

---

**自分だけの学習支援 AI を作ろう！** 🚀
